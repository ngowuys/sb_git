name: sb_git
requiredPermissions:
  - shell
functions:
  gitCloneCmd:
    path: "sb_git:gitClone"
    command:
      name: "Git: Clone repo"
  syncFromGitCmd:
    path: "sb_git:sync"
    command:
      name: "Git: Sync from Git"
  replaceTokenCmd:
    path: "sb_git:replaceToken"
    command:
      name: "Git: Replace token"
  changeGitRepoCmd:
    path: "sb_git:changeGitRepo"
    command:
      name: "Git: Change git repo"
  scheduleCommitCmd:
    path: "sb_git:scheduleCommit"
    env: server
    cron: "* * * * *"
